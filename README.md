# Spring Boot Stripe Gateway 🚀

A **Spring Boot** application that serves as a **gateway for Stripe APIs**.  
It provides a backend layer to handle **Payments, Invoices, and Subscriptions**, ensuring secure integration between clients (web/mobile apps) and Stripe.

---

## 🎯 Features
- Integration with **Stripe Java SDK**.